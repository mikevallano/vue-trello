<p id="notice"><%= notice %></p>

<%= javascript_pack_tag 'lists_vue' %>

<h1>Lists</h1>

<lists></lists> <!-- lists is the name of the component included in the Vue object created in lists_vue.js -->

<%= tag.div id: :boards,
    data: { lists: @lists.to_json(include: :cards) } do %>
  {{testmessage}} <!-- this comes from the data of the Vue object created in lists_vue.js -->
<% end %>


<br/>
<div class='mt-5'>
  <%= link_to 'New List', new_list_path, class: 'btn btn-secondary' %>
</div>
